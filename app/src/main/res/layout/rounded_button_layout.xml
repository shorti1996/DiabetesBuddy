<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent" android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    tools:background="@android:color/black">

    <View
        android:id="@+id/take_photo_rounded_button"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:background="@drawable/circle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/take_photo_icon"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:background="@drawable/ic_baseline_photo_camera_24"
        app:layout_constraintBottom_toBottomOf="@+id/take_photo_rounded_button"
        app:layout_constraintEnd_toEndOf="@+id/take_photo_rounded_button"
        app:layout_constraintStart_toStartOf="@+id/take_photo_rounded_button"
        app:layout_constraintTop_toTopOf="@+id/take_photo_rounded_button" />
</androidx.constraintlayout.widget.ConstraintLayout>